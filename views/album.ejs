<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>
    <%= album.title %> - Album Detail
  </title>
  <style>
    body {
      margin: 0;
      background: #f5f6fa;
    }

    .body {
      font-family: Arial, sans-serif;
      background: #f5f6fa;
      margin: 0;
      padding: 20px;
    }

    .container {
      max-width: 1000px;
      margin: auto;
      background: #fff;
      padding: 20px;
      border-radius: 15px;
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
    }

    h1 {
      text-align: center;
      margin-bottom: 10px;
      color: #333;
    }

    .meta {
      text-align: center;
      margin-bottom: 20px;
      font-size: 15px;
      color: #555;
    }

    .status {
      display: inline-block;
      padding: 6px 12px;
      border-radius: 20px;
      font-size: 14px;
      font-weight: bold;
    }

    .public {
      background: #2ecc71;
      color: white;
    }

    .private {
      background: #e74c3c;
      color: white;
    }

    .gallery {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
      gap: 15px;
    }

    .gallery-item {
      background: #fafafa;
      border-radius: 12px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
      overflow: hidden;
      text-align: center;
      transition: transform 0.3s;
    }

    .gallery-item:hover {
      transform: translateY(-5px);
    }

    .gallery-item img {
      width: 100%;
      height: 180px;
      object-fit: cover;
    }

    .caption {
      padding: 10px;
    }

    .caption h3 {
      margin: 5px 0;
      font-size: 16px;
      color: #333;
    }

    .caption p {
      font-size: 13px;
      color: #777;
    }

    .back-btn {
      display: inline-block;
      margin-top: 25px;
      text-decoration: none;
      background: #3498db;
      color: white;
      padding: 10px 18px;
      border-radius: 8px;
      transition: background 0.3s;
    }

    .back-btn:hover {
      background: #2980b9;
    }
  </style>
</head>

<body>

  <%- include('partials/header') %>

    <div class="body">
      <div class="container">
        <!-- ข้อมูลอัลบั้ม -->
        <h1>
          <%= album.title %>
        </h1>
        <div class="meta">
          <p>
            <%= album.content || "ไม่มีรายละเอียด" %>
          </p>
          <% if (album.status==="private" ) { %>
            <span class="status private">Private</span>
            <% } else { %>
              <span class="status public">Public</span>
              <% } %>
        </div>

        <!-- รูปในอัลบั้ม -->
        <div class="gallery">
          <% album.photos.forEach(photo=> { %>
            <div class="gallery-item">
              <a href="/photo/<%= photo.id %>">
                <img src="<%= photo.imageUrl || ('/uploads/' + photo.filename) %>"
                  alt="<%= photo.title || photo.originalname || 'photo' %>" loading="lazy" />
              </a>
              <div class="caption">
                <h3>
                  <%= photo.title %>
                </h3>
                <p>
                  <%= photo.content || "ไม่มีรายละเอียด" %>
                </p>
              </div>
            </div>
            <% }) %>
        </div>

        <!-- ปุ่มกลับ -->
        <div style="text-align: center;">
          <a href="#" class="back-btn"
            onclick="if (history.length > 1) { history.back(); } else { window.location = '/home'; } return false;">←
            Back</a>
        </div>
      </div>
    </div>
</body>

</html>